#include<iostream>
using namespace std;
int main (){
    int n ;
    cout<<"enter n : ";
    cin>>n;
    if(n%2==0) cout<<"the no is even"; ///2 se divisible hai means even hai  
    else cout<<"the no is odd :  "  ;

    //this is the one way to write the code next code - [oddeven(5)] is the second syntax

}

